To Run:
python3 run.py

Requirements:
The only requirement is numpy, which can be installed by entering the "pip3 install numpy" into the command line

Settings:
All settings can be changed in settings.py and are explained there

About:
SnakeAI is a genetic algorithm and neural network designed to learn to play the game of snake.
